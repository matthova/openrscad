// The same drawing at a coarse setting, to pin that the caller's fragments
// really drive the imported curve and not just the default.
//
// oracle: tris
$fa = 30;
linear_extrude(1) import("arcs.dxf");
