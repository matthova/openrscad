// `import_stl()` is the deprecated 2021.01 spelling of `import()`. OpenSCAD
// keeps it as a plain alias — the format still comes from the file suffix, not
// from the module name — plus a deprecation notice, so this must match
// import_stl.scad exactly.
import_stl("cube.stl");
