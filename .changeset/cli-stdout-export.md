---
"openrscad-release-root": minor
---

`-o -` writes the export to stdout (requires `--export-format`), suppressing echoes so they cannot corrupt the byte stream — enabling `openrscad model.scad -o - --export-format binstl | …`
