---
"openrscad-release-root": minor
---

import binary STL and 3MF meshes in the browser — drop or open one and reference it with `import("file.stl")` (previously only text/ASCII formats loaded outside the desktop app)
