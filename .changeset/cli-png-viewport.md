---
"openrscad-release-root": minor
---

CLI PNG rendering now honors a script-set `$vpr`/`$vpt`/`$vpd`/`$vpf` viewport as the camera (fixes rendering from the wrong angle), and adds `--colorscheme` (background) and `--animate_sharding i/n`
